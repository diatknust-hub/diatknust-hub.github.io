# Environment variables — NEVER commit these
.env
.env.local
.env.*.local

# Cloudinary local-reference file — regenerated by tools/cloudinary-sync.mjs,
# not needed in the repo (all the URLs it contains are already in the HTML)
cloudinary-ref.json

# Backup files created during CDN migration scripts
*.bak-cdn

# Node modules
node_modules/

# npm debug
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
